# 发布执行备注

这个文件写给发布执行人看的补充规则。团队主要发布信息优先填写 `简易发布配置.md`；如果没有这个文件，则直接维护 `config.json`。

## common

- 如果 `简易发布配置.md` 存在，先同步生成 `config.json`；如果不存在，直接使用 `config.json`。
- 只发布 `config.json` 里 `enabledPlatforms` 指定的平台。
- 不根据平台文件夹是否有素材自动发布。
- 不从 `asset/` 直接取素材发布，只从各平台目录读取最终版本。
- 启用平台的 `video/` 里必须只有 1 个最终视频。
- 启用平台的 `cover/` 里必须只有 1 张最终封面。
- 不跳过 `validate`。
- 不跳过 `publish --dry-run`。
- 未确认前不正式发布。

## douyin

覆盖抖音执行备注。没有特殊要求时保持为空。

- 

## kuaishou

覆盖快手执行备注。没有特殊要求时保持为空。

- 

## xiaohongshu

覆盖小红书执行备注。没有特殊要求时保持为空。

- 
